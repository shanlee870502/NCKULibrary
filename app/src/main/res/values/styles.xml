<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">  深色-->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar"> <!--無深色-->
        <!-- Customize your theme here. -->
        <item name="windowActionModeOverlay">true</item>
    </style>
    <style name="AppTheme.NoActionBar" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>
    <style name="subDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">true</item>

    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
